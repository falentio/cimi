{
  "name": "cimi",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "db:check": "pnpm --filter @cimi/db run db:check",
    "db:generate": "pnpm --filter @cimi/db run db:generate",
    "db:push": "pnpm --filter @cimi/db run db:push",
    "db:studio": "pnpm --filter @cimi/db run db:studio",
    "prepare": "vp config --no-agent"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24.16.0 <25"
  },
  "volta": {
    "node": "24.16.0",
    "pnpm": "11.18.0"
  },
  "packageManager": "pnpm@11.18.0"
}
