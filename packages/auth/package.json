{
  "name": "@cimi/auth",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "auth:generate": "better-auth generate --config src/auth.codegen.ts --adapter drizzle --dialect sqlite --output ../db/src/schema/auth.generated.ts --yes",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "better-auth": "catalog:"
  },
  "devDependencies": {
    "@cimi/db": "workspace:*",
    "@types/node": "catalog:",
    "auth": "catalog:",
    "drizzle-orm": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "drizzle-orm": "catalog:"
  }
}
